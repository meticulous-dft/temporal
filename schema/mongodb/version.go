package mongodb

const (
	// Version is the MongoDB default persistence schema release version.
	Version = "1.1"

	SchemaVersionCollection       = "schema_version"
	SchemaUpdateHistoryCollection = "schema_update_history"
)
